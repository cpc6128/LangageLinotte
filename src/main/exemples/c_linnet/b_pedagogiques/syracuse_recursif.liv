/*Auteur : $imon*/
n :: number
input n
n = abs( round( n ))  //pour s'assurer qu'il s'agit bien d'un entier positif
call syracuse with n
print "la durée du vol est " + n
//exit

syracuse : x
	if (round(x / 2)) = (x / 2), x = x / 2
	else x = 3 * x + 1
	if x > 1 call syracuse with x
	else x = 0
	x = x + 1
	return