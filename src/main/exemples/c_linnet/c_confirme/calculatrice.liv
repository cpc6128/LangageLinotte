// Calculatrice simple :
// + - ajoute ou soustrait 1
// * / multiplie ou divise par 2
programme := "+", "+", "*", "-", "/"
accumulateur := 0
for programme  call machine with accumulateur, joker
print "Le programme a calculé la valeur {accumulateur}"

machine : sortie, commande
	if commande = "+" increment sortie
	else if commande = "-" decrement sortie
	else if commande = "*", sortie = sortie * 2
	else if commande = "/", sortie = sortie / 2
	return